TMDB_API_ACCESS_TOKEN=

# Extra hosts allowed to call the /api routes cross-origin (comma-separated).
# movieshelf.app and www.movieshelf.app are always allowed. Leave empty in prod.
ALLOWED_API_HOSTS =
